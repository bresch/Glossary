clean:
	rm -f *.log
	rm -f *.drt
	rm -f *.glo
	rm -f *.gls
	rm -f *.glg
	rm -f *.aux
	rm -f *.slg
	rm -f *.nlg
	rm -f *.gsl
	rm -f *.fdb_latexmk
	rm -f *.dos
	rm -f *.gsg
	rm -f *.gso
	rm -f *.syi
	rm -f *.zld
	rm -f *.not
	rm -f *.ntn
	rm -f *.dog
	rm -f *.fls
	rm -f *.ist
	rm -f *.acr
	rm -f *.acn
	rm -f *.alg
